#lang info

(define collection 'multi)

(define deps '("rackunit"
               "wxme-lib"
               "base"
               "snip-lib"))

(define pkg-desc "tests for \"wxme\"")

(define pkg-authors '(mflatt))
